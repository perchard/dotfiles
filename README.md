1. Copy this repository to the home directory (i.e.`~/dotfiles`).
2. Install GNU Stow (`brew install stow`).
3. From within the `~/dotfiles` directory, run `stow --dotfiles .` (which will create symlinks automatically).
